server.port=8080

# Allowed origin for the React dev server (CRA default port)
app.cors.allowed-origin=http://localhost:3000

# Basic logging so you can see WebSocket connects/messages while testing
logging.level.com.chatapp=INFO
logging.level.org.springframework.web.socket=INFO
